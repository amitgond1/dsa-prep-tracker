# Server (root .env)
MONGO_URI=mongodb://127.0.0.1:27017/dsa-tracker
JWT_SECRET=replace_with_strong_secret
PORT=5000
# Comma-separated allowed origins supported (local + production)
CLIENT_URL=http://localhost:5173
NODE_ENV=development
